This is the directory into which the neuronet library can be copied to
in order to "attach" it to the neuro library.

You can clone the repository directly inside this directory by doing :


Copy the whole project's files, including all the configure tools and
files. The command lndir could also be used to do it too.


